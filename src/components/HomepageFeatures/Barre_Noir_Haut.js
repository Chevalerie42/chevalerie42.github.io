import React from 'react';
import clsx from 'clsx';
import styles from './styles.module.css';

export default function Barre_Noir_Haut () 
{
    return (
        <div 
            className={clsx('padding-top--xs')}
            style={{backgroundColor: 'rgb(3, 3, 3)'}}
        >
        </div>
      );
}
